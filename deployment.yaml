template-file-path: security-baseline-level4.yaml

stack-name: bem11-security-baseline-level4-lab

region: us-east-1

capabilities:
  - CAPABILITY_IAM
  - CAPABILITY_NAMED_IAM

parameters:
  FullName: "Timothy Nlenjibi"

tags:
  Project: BEM11-Security-Baseline-Lab
  Environment: Lab
  ManagedBy: GitSync-CloudFormation
  ControlLevel: Level4-LeastPrivilege-VPC-Endpoints